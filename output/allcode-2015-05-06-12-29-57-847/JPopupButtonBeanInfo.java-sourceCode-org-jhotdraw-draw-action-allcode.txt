org jhotdraw draw action import beans * popup button bean info simple bean info { private bean descriptor bean descriptor = private bean descriptor get bdescriptor { if bean descriptor == bean descriptor = new bean descriptor popup button bean info bean descriptor set value container bean descriptor set display popup button bean descriptor } private property descriptor properties = private property descriptor get pdescriptor { properties } private event set descriptor event sets = private event set descriptor get edescriptor { event sets } private descriptor = private descriptor get mdescriptor { } private awt image icon color16 = private awt image icon color32 = private awt image icon mono16 = private awt image icon mono32 = private icon c16 = private icon c32 = private icon m16 = private icon m32 = private default property index = -1 private default event index = -1 bean descriptor get bean descriptor { get bdescriptor } property descriptor get property descriptors { get pdescriptor } event set descriptor get event set descriptors { get edescriptor } descriptor get descriptors { get mdescriptor } get default property index { default property index } get default event index { default event index } awt image get icon icon kind { switch icon kind { case ICON_COLOR_16x16 if icon c16 == else { if icon color16 == icon color16 = load image icon c16 icon color16 } case ICON_COLOR_32x32 if icon c32 == else { if icon color32 == icon color32 = load image icon c32 icon color32 } case ICON_MONO_16x16 if icon m16 == else { if icon mono16 == icon mono16 = load image icon m16 icon mono16 } case ICON_MONO_32x32 if icon m32 == else { if icon mono32 == icon mono32 = load image icon m32 icon mono32 } default } } } 