nanoxml Xml Parse Exception Runtime Exception { NO_LINE = -1 private line Nr Xml Parse Exception String String message { super Xml Parse Exception during parsing of + == ? the Xml definition + + element + + message this line Nr = Xml Parse Exception NO_LINE } Xml Parse Exception String line Nr String message { super Xml Parse Exception during parsing of + == ? the Xml definition + + element + line + line Nr + + message this line Nr = line Nr } get Line Nr { this line Nr } } 