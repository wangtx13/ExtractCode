net n3 nanoxml import reader import IOException PIReader reader reader { private IXMLReader xml reader reader private atEndOfData of data PIReader reader IXMLReader xml reader reader { this reader = reader this atEndOfData of data = } finalize throwable { this reader = super finalize } read char buffer offset size IOException { if this atEndOfData of data { 1 } charsRead chars read = 0 if offset + size > buffer length { size = buffer length offset } while charsRead chars read < size { char ch = this reader read if ch == '?' { char ch2 = this reader read if ch2 == '>' { this atEndOfData of data = break } this reader unread ch2 } buffer charsRead chars read = ch charsRead++ chars read++ } if charsRead chars read == 0 { charsRead chars read = 1 } charsRead chars read } close IOException { while ! this atEndOfData of data { char ch = this reader read if ch == '?' { char ch2 = this reader read if ch2 == '>' { this atEndOfData of data = } } } } } 