org jhotdraw samples svg import import org jhotdraw draw * import org jhotdraw samples svg figures * import org jhotdraw xml * drawing default drawing { private title private description drawing { } set title new value { old value = title title = new value fire property change title old value new value } get title { title } set description new value { old value = description description = new value fire property change description old value new value } get description { description } read dom input { for i=0 = get element count < i++ { open element = get tag if equals title { title = get text close element } else if equals desc { description = get text close element } else if equals defs { close element } else if equals use { close element } else if equals script { close element } else if equals style { close element } else if equals radial gradient { close element } else { close element = read if instanceof drawing { group = new group will change for figure child drawing get figures { basic add child } changed if ! empty { add } } else if instanceof figure { if ! figure empty { add figure } } else { new unexpected child +f } } } read attributes } read attributes dom input { } @ write dom output out { out add attribute xmlns http out add attribute 1 2 out add attribute base profile tiny for figure get figures { out write } } } 