org jhotdraw samples svg figures import awt * import awt geom * import org jhotdraw draw * import org jhotdraw samples svg * image attributed figure implements figure { private rectangle2 bounds = new rectangle2 image { util set defaults this } draw fill graphics2 { fill bounds } draw stroke graphics2 { draw bounds } basic transform affine transform ty { } basic set bounds point2 start point2 { } rectangle2 get bounds { rectangle2 bounds clone } get restore data { get bounds } restore to restore data { bounds = rectangle2 rectangle2 restore data clone } contains point2 { bounds contains } image clone { image that = image super clone that bounds = rectangle2 bounds clone that } empty { } } 