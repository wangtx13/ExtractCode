nanoxml xml parse runtime { NO_LINE = 1 private line nr xml parse name message { super xml parse during parsing of + name == ? the xml definition a + name + element + + message this line nr = xml parse NO_LINE } xml parse name line nr message { super xml parse during parsing of + name == ? the xml definition a + name + element + at line + line nr + + message this line nr = line nr } get line nr { this line nr } } 