org jhotdraw samples svg figures import awt * import awt geom * import org jhotdraw draw * import org jhotdraw samples svg * s v g image attributed figure implements s v g figure { private rectangle2 d bounds = new rectangle2 d s v g image { s v g util set defaults this } draw fill graphics2 d g { g fill bounds } draw stroke graphics2 d g { g draw bounds } basic transform affine transform ty { } basic set bounds point2 d start point2 d end { } rectangle2 d get bounds { rectangle2 d bounds clone } get restore data { get bounds } restore to restore data { bounds = rectangle2 d rectangle2 d restore data clone } contains point2 d p { bounds contains p } s v g image clone { s v g image that = s v g image super clone that bounds = rectangle2 d bounds clone that } is empty { } } 