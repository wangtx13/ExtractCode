org jhotdraw draw import org jhotdraw geom bezier path import javax swing undo undoable edit import javax swing undo cannot redo import javax swing undo cannot undo import javax swing undo undoable edit bezier node edit undoable edit { private bezier figure owner private index private bezier path node old value private bezier path node new value bezier node edit bezier figure owner index bezier path node old value bezier path node new value { this owner = owner this index = index this old value = old value this new value = new value } get presentation name { punkt verschieben } redo cannot redo { super redo owner will change owner basic set node index new value owner changed } undo cannot undo { super undo owner will change owner basic set node index old value owner changed } add edit undoable edit an edit { if an edit instanceof bezier node edit { bezier node edit that = bezier node edit an edit if that owner == this owner that index == this index { this new value = that new value that die } } } replace edit undoable edit an edit { if an edit instanceof bezier node edit { bezier node edit that = bezier node edit an edit if that owner == this owner that index == this index { that old value = this old value this die } } } } 